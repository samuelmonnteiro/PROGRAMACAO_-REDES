# Solicita ao usuário que insira um número de até 4 dígitos
numero = input("Digite um número de até 4 dígitos (0 a 9999): ")

# Verifica se o número está dentro do intervalo permitido
if  0 <= int(numero) <= 9999:
    soma = 0  # Variável para armazenar a soma dos algarismos

    # Para cada caractere do número, converte para inteiro e soma
    for algarismo in numero:
        soma += int(algarismo)

    # Exibe o resultado
    print("A soma dos algarismos é:", soma)
else:
    print("Número inválido! Por favor, insira um número entre 0 e 9999.")
